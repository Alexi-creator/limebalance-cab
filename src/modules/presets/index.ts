/** Public entry of the module: types and the filter-set helpers the tables build their presets
 *  with. The component comes from `@/modules/presets/ui`. */
export { canonicalizeFilters } from "./lib/canonical"
export type { FilterPresetScope, PresetFilters, PresetFilterValue } from "./model"
