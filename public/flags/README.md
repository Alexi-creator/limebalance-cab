Circle flag icons from https://github.com/HatScripts/circle-flags (MIT).
Only the countries the interface languages map to are vendored here — see
`region` in src/shared/i18n/languages.ts. Files are named by ISO 3166-1 alpha-2.
